# Marketing agent environment variables
# Copy to .env if needed. No env vars are required at runtime —
# bot token + channel ID live in .mcp.json; agent identity / task files
# live in the bind-mounted source project (see docker-compose.yml).
